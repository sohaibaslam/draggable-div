# Docs for draggable-div react project

Used `react-draggable` library to manage draggable component.

## Available Scripts
To run the project execute following in the project directory:

### `yarn install`
To install the dependencies

### `yarn start`
To run the project